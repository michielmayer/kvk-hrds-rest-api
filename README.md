HR Dataservice Rest API
----------------------

Install:

```
npm install
```

Run

```
node index.js
```

Or run with pm2:

```
pm2 start index.js --name="hrds" --log-date-format "YYYY-MM-DD HH:mm Z"
```